import React from "react";

const MusicInput = ({ value, onChange }) => {
  return (
    <div>
      <label htmlFor="music">music</label>
      {/* <textarea id="music" value={value} onChange={onChange} /> */}
      <p>iframe here</p>
    </div>
  );
};

export default MusicInput;